// ============================================================================
// claude-session-kit — Persistent Claude Agent SDK sessions (streaming-input mode): keep the CLI subprocess alive across turns for 8x prompt-cache efficiency, with per-turn results, interrupt/setModel, cache stats, and a per-project session pool.
// ----------------------------------------------------------------------------
// 零依賴（node 內建）。factory pattern。
// ============================================================================

module.exports = function create(opts = {}) {
  // TODO: 實作。把 app-specific 的東西用 opts 注入，只留通用核心。
  return {};
};
